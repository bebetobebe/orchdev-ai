# 安全策略

## 支持范围

当前项目处于 `0.0.x` 早期版本，默认只承诺维护主分支和最新打包版本。历史版本、私人分支和本地二次修改版本不在默认支持范围内。

## 报告安全问题

如果你发现以下问题，请优先私下联系维护者，不要直接公开完整利用细节：

- API 密钥、令牌或本地凭据泄露
- 任务派发导致非预期命令执行
- VSIX 打包内容包含不应发布的文件
- Webview 中可被外部输入触发的脚本注入
- MCP 客户端、固定 API 工具桥或命令行执行器存在权限边界绕过

如果仓库暂时没有公开安全通道，可以先在 issue 中只描述影响范围，不贴敏感样例、真实密钥、完整攻击脚本或私有服务地址。

## 不要公开的内容

- 真实 API Key、Bearer Token、Cookie、访问凭据
- 本机绝对路径、公司内网地址、私有中继地址
- 可直接复现攻击的最小 payload
- 带敏感参数的日志、截图或录屏

## 密钥处理原则

- 中继服务令牌通过 `OrchDev AI：设置中继服务令牌` 保存到 VS Code 系统密钥存储。
- 固定 API 密钥通过 `OrchDev AI：设置固定 API 密钥` 保存到 VS Code 系统密钥存储。
- 不要把密钥写入 `settings.json`、源码、README、测试快照、issue 或 PR 讨论。
- `src/config/relayConfig.ts` 与 `src/config/fixedApiConfig.ts` 只允许放公开可见的地址、模型和能力开关，不允许硬编码真实密钥。

## 工作区工具边界

- 内置工作区工具默认拒绝访问明显敏感的本地路径，例如 `.env`、`.git`、`node_modules`、构建缓存目录，以及常见密钥、证书、凭据文件。
- `Ask` / `Plan` 模式只暴露只读工具；只有 `Execute` 模式才允许写文件。
- 文件写入有默认大小上限，避免模型误写入超大内容；复杂修改应拆成多步完成。
- 本地命令执行默认关闭，只有显式开启 `allowCommandExecution` 后才会暴露命令工具。
- 安全自检任务默认只写入 `.ai-orchestrator/self-check.md`。

## 本地安装建议

安装本地 VSIX 前建议运行：

```bash
npm audit
npm run verify
npm run vsix
```

当前 `.vscodeignore` 会排除源码、测试、`node_modules`、旧 `.vsix` 和开发辅助文件，保留运行时 bundle、README、文档、许可证、更新日志与前端资源。

## 处理预期

由于项目当前以开源自用为主，安全问题的处理节奏可能不是企业级 SLA，但建议维护者至少做到：

1. 先确认影响范围。
2. 先移除或封堵高风险公开入口。
3. 修复后更新 `CHANGELOG.md` 或 Release Notes。
4. 必要时提醒用户重新生成或轮换已暴露密钥。
