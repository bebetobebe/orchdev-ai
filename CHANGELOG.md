# 更新日志

本文档记录用户可见的重要变化。当前项目处于早期版本，版本节奏以可安装、可验证、可自用为优先。

## [0.0.1] - 2026-05-12

首个面向“开源仓库 + 本地 VSIX 自用”形态整理完成的版本。

### 新增

- 提供 VS Code 主面板，用会话和任务组织复杂开发工作。
- 支持任务创建、编辑、派发、自动派发、取消、重试、复制、删除和会话导出。
- 支持每个执行器独立队列、自动接续和自动重连扫描。
- 支持 Codex、OpenCode、Claude Code、Gemini、Aider 命令行执行器。
- 支持 MCP 客户端执行器，以及默认关闭的内置 MCP 占位执行器。
- 支持内置 HTTP 中继服务执行器；开源自用版默认关闭，需要源码中配置真实中继地址后再启用。
- 支持固定 OpenAI 兼容 API，地址、模型和能力开关在源码中固定，密钥保存到系统密钥存储。
- 增加“启用固定 API”命令，安装后只需保存密钥即可注册固定执行器。
- 增加“测试固定 API 连接”命令和面板入口，可同时检测基础响应与工具调用能力。
- 增加“创建安全自检任务”命令和面板入口，可验证执行器调度、工具调用、项目读取和安全写入链路。
- 完成主要界面、命令、README 和辅助文档的中文化，并补充英文 README。

### 增强

- 固定 API 兼容 Chat Completions 与 Responses 两种请求协议。
- Responses API 执行链支持工具调用与 `function_call_output` 回填。
- Chat Completions 工具调用链兼容旧式 `function_call`，并在网关不支持 `tool_choice` 时自动降级重试。
- 执行模式更明确要求固定 API 使用工作区写入工具完成真实文件修改，减少只返回文字说明的情况。
- 工作区工具支持批量读取文件、按行范围替换、按行范围删除，并提高复杂任务默认工具调用轮次。
- 工作区工具读文件时返回行号，便于模型做精确替换。
- 监听工作区文件夹变化，切换或打开项目后会自动重建执行器，确保文件工具使用当前项目根目录。
- 执行器卡片新增能力标签，可直观看到项目读取、执行可写、本地命令、只读沙箱、仅文本响应等状态。
- 固定 API 测试结果直接显示在执行器卡片上，执行任务派发到明显不可写执行器时会给出中文提示。
- 任务结果中的“修改文件”支持点击打开工作区文件，并拦截项目外路径。
- 固定 API 执行任务完成但没有检测到文件修改时，任务卡片会显示中文提醒。
- 侧边栏视图标题和欢迎区增加打开面板、启用固定 API、测试固定 API、安全自检等直达入口。
- 支持中断识别、中文恢复建议和临时错误自动延迟重试。

### 安全

- 工作区工具默认拒绝访问 `.env`、`.git`、`node_modules`、构建缓存和常见密钥/证书文件。
- 允许安全自检写入 `.ai-orchestrator/self-check.md`，同时继续拦截其它隐藏配置目录。
- 本地命令执行默认关闭，只有显式开启 `allowCommandExecution` 才会暴露命令工具。
